en:
  application:
    multi_factor_authentication_required: |
      You do not currently have two-factor authentication enabled.
      You will need to set it up before you can use this application.
  users:
    sessions:
      mfa_field_label: "Two-factor authentication code (or backup code)"
  multi_factor_authentications:
    show:
      new_multi_factor_authentication: Manage two-factor authentication
      create_backup_codes: Generate backup codes
      destroy_backup_codes: Delete my current backup codes
    destroy_backup_codes:
      success: Backup codes successfully deleted
      delete_backup_codes: Delete my current backup codes
